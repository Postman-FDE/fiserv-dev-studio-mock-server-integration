$kind: http-example
request:
  url: '{{base_url}}/hello'
  method: GET
response:
  statusCode: 200
  statusText: OK
  headers:
    - key: Content-Type
      value: application/json
  body:
    type: json
    content: |-
      {
        "message": "hello from mock-server-integration"
      }
